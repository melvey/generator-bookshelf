
module.exports {
	client: '<%= connection %>',
	connection: {
<% if(credentials) { %>
		host: '<%= host %>',
		user: '<%= user %>',
		password: '<%= password %>',
		database: '<%= dbname %>',
		charset: 'utf8'
<% } else { %>
		filename: '<%= filename %>',
<% } %>
	},
	pool: {
		min: 1,
		max: 7
	},
	migrations: {
		directory: '../../scripts/database'
	}
};
